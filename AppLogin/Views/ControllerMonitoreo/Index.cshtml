@model IEnumerable<AppLogin.Model.Monitoreo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaMonitoreo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDescripcionMonitoreo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCultivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Eliminado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaMonitoreo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDescripcionMonitoreo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCultivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Eliminado)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdMonitoreo">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdMonitoreo">Details</a> |
                    
            </td>
        </tr>
}
    </tbody>
</table>
